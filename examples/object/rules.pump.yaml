rules:
  - name: service-shape
    match: "$.*"
    defaults:
      replicas: 1
      env: prod
      resources:
        cpu: 100m
        memory: 128Mi
      telemetry:
        logs: true
        metrics: true
